@model List<TableTest>
@{
    ViewBag.Title = "Home Page";
}

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>ID</th>
            <th>FullName</th>
            <th>Phone</th>
            <th>Address</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.TableId</td>
                <td>@item.FullName</td>
                <td>@item.Phone </td>
                <td>@item.Address</td>
            </tr>
        }
    </tbody>
</table>
